<resources>

    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppBottomSheetDialogTheme" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/AppModalStyle</item>
    </style>

    <style name="AppModalStyle" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@drawable/rounded_top_white</item>
    </style>

    <style name="ActionBarThemeOverlay">
        <item name="colorControlNormal">@color/white</item>
    </style>

    <style name="ProgressSeekBar">
        <item name="android:progress">0</item>
        <item name="android:secondaryProgress">0</item>
        <item name="android:paddingStart">0dp</item>
        <item name="android:paddingEnd">0dp</item>
        <item name="android:progressDrawable">@drawable/mp_seekbar</item>
        <item name="android:minHeight">50dp</item>
        <item name="android:maxHeight">50dp</item>
        <item name="android:thumb">@null</item>
        <item name="android:background">@null</item>
    </style>

    <style name="TextPrimary">
        <item name="android:textColor">@color/colorPrimaryText</item>
    </style>

    <style name="TextPrimary.20">
        <item name="android:textSize">20sp</item>
    </style>

</resources>
